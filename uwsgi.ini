[uwsgi]
chdir = /root/workDir/info_sec/info_sec_server/info_sec_server/
module = info_sec_server.wsgi
home = /data/env/djangoenv
master          = true
# maximum number of worker processes
processes       = 5
# the socket (use the full path to be safe
#指定socket协议，运行django，只能与nginx结合时使用
#指定socket协议，运行django，只能与nginx结合时使用
#指定socket协议，运行django，只能与nginx结合时使用
socket          = 0.0.0.0:8008